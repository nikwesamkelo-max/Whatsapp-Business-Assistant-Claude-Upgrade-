# Whatsapp-Business-Assistant-Claude-Upgrade-
replaces the rule-based brain with Claude, tracks each customer's conversation separately by phone number, and makes bookings real (persisted to SQLite) instead of simulated.
